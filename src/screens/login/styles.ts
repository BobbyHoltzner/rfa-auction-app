import {StyleSheet,  FlexJustifyType} from "react-native";

export default StyleSheet.create({
    container: {
        flex: 1,
        paddingHorizontal: 24,
        justifyContent: "flex-start" as FlexJustifyType,
        backgroundColor: "#ffffff"
    }
});